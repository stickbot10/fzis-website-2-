{
  "name": "firebase-hosting-template",
  "version": "1.0.0",
  "description": "Firebase Hosting Template",
  "scripts": {
    "build": "cp -r public/* dist/",
    "deploy": "firebase deploy",
    "serve": "firebase serve"
  },
  "dependencies": {
    "firebase": "^11.6.1"
  },
  "devDependencies": {
    "firebase-tools": "^12.0.0"
  }
} 